<script type="module">
    import {
        auth
    } from './js/authentication/auth.js'

    auth.onAuthStateChanged(user => {
        if (user) {
            window.location.href = '/admin'
        } else {
            window.location.href = '/public'
        }
    })
</script>